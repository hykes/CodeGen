package {{packageName}}.service;

import club.itbus.core.model.Response;
import club.itbus.core.model.Paging;
import {{packageName}}.model.{{modelName}};

import java.util.Map;
/**
 * Desc:
 * Author:
 * Date: {{createdAt}}
 */
public interface {{serviceName}} {

    /**
     * 查询
     * @param id
     * @return {{LowerCase modelName}}
     */
    Response<{{modelName}}> load(Long id);

    /**
     * 分页
     * @param pageNo
     * @param pageSize
     * @param criteria
     * @return Paging<{{modelName}}>
     */
    Response<Paging<{{modelName}}>> paging(Integer pageNo, Integer pageSize, Map<String, Object> criteria);

    /**
     * 创建
     * @param {{LowerCase modelName}}
     * @return Boolean
     */
    Response<Boolean> create({{modelName}} {{LowerCase modelName}});

    /**
    * 修改
    * @param {{LowerCase modelName}}
    * @return Boolean
    */
    Response<Boolean> update({{modelName}} {{LowerCase modelName}});
}