
import io.terminus.common.model.Response;

/**
 *
 * @author: ${email}
 * @date: ${Now}
 * Created by CodeGen .
 */
public interface ${model}WriteService {

    /**
     * 创建
     * @param ${model}
     * @return Boolean
     */
    Response<Long> create(${model} ${model});

    /**
     * 更新
     * @param {{LowerCase model}}
     * @return Boolean
     */
    Response<Boolean> update(${model} ${model});

    /**
     * 删除
     * @param id
     * @return Boolean
     */
    Response<Boolean> delete(Long id);

}