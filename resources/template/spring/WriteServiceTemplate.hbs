package {{#if $.Package1}}{{$.Package1}}.{{/if}}service;

import {{#if $.Package1}}{{$.Package1}}.{{/if}}model.{{model}};

import io.terminus.common.model.Response;

/**
 *
 * @author: {{$.email}}
 * @date: {{$.Now}}
 * Created by CodeGen .
 */
public interface {{model}}WriteService {

    /**
     * 创建
     * @param {{LowerCase model}}
     * @return Boolean
     */
    Response<Long> create({{model}} {{LowerCase model}});

    /**
     * 更新
     * @param {{LowerCase model}}
     * @return Boolean
     */
    Response<Boolean> update({{model}} {{LowerCase model}});

    /**
     * 删除
     * @param id
     * @return Boolean
     */
    Response<Boolean> delete(Long id);

}