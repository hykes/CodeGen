
#ImportPackage(${model})

/**
 * $!{comment}写服务接口
 * @author $!{email}
 * @date ${NOW}
 * Created by CodeGen .
 */
public interface ${model}WriteService {

    /**
     * 创建$!{comment}
     * @param #LowerCase(${model})

     * @return Boolean
     */
    Long create(${model} #LowerCase(${model}));

}